"use client";

import { SectionIndex } from "@/components/layout/SectionIndex";

// A section heading in the sidebar, not a page. Typing this URL used to 404.
export default function LabKnowledgeIndexPage() {
  return <SectionIndex section="Lab Knowledge" />;
}
