pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-xdist>=3.5
ruff==0.15.17  # pinned: ruff 0.16 turned on new default rules that flag the existing tree (2397 findings); unpin deliberately after a repo-wide cleanup
mypy==2.1.0    # pinned alongside ruff so an unpinned linter release cannot break CI again
httpx>=0.27
factory-boy>=3.3
respx>=0.21
